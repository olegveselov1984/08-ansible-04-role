---
- name: Install Clickhouse
  hosts: clickhouse
  become: true
  handlers:
    - name: Start clickhouse service
      ansible.builtin.service:
        name: clickhouse-server
        state: started
      tags:
        - clickhouse
        - start service
  tasks:
    - block: #Нужен, поскольку есть "rescue - что не отработало в 01 будет работать в 02"
       - name: Get clickhouse distrib01
         ansible.builtin.get_url:
            url: "https://packages.clickhouse.com/rpm/stable/{{ item }}-{{ clickhouse_version }}.noarch.rpm"
            dest: "./{{ item }}-{{ clickhouse_version }}.rpm"
            mode: '0644' # Права выжны!
         with_items: "{{ clickhouse_packages }}"
         tags:
            - clickhouse
            - distr
      rescue:
       - name: Get clickhouse distrib02
         ansible.builtin.get_url:
            url: "https://packages.clickhouse.com/rpm/stable/clickhouse-common-static-{{ clickhouse_version }}.x86_64.rpm"
            dest: "./clickhouse-common-static-{{ clickhouse_version }}.rpm"
            mode: '0644' # Права выжны!
         tags:
            - clickhouse
            - distr

       - name: Install clickhouse packages
         become: true
         ansible.builtin.yum:
           name:
            - clickhouse-common-static-{{ clickhouse_version }}.rpm
            - clickhouse-client-{{ clickhouse_version }}.rpm
            - clickhouse-server-{{ clickhouse_version }}.rpm
         notify: Start clickhouse service
         tags:
            - clickhouse
            - distr

       - name: Flush handlers
         ansible.builtin.meta: flush_handlers
         tags:
            - clickhouse
            - start service

       - name: Create database
         ansible.builtin.command: "clickhouse-client -q 'create database logs;'"
         register: create_db
         failed_when: create_db.rc != 0 and create_db.rc !=82
         changed_when: create_db.rc == 0
         tags:
            - clickhouse
            - db


- name: Install Vector
  hosts: vector
  become: true
  handlers:
    - name: Start vector service
      ansible.builtin.service:
        name: vector
        state: restarted
      tags:
        - vector
        - restart service

  tasks:
    - name: Get vector distrib
      ansible.builtin.get_url:
        url: https://packages.timber.io/vector/{{ vector_version }}/vector-{{ vector_version }}-1.x86_64.rpm
        dest: ./{{ vector_version }}-1.x86_64.rpm
      tags:
        - vector
        - distr

    # - name: Update apt cache # Зачем блок?
    #   ansible.builtin.apt:   # Зачем блок?
    #     update_cache: true    # Зачем блок?

    - name: Install vector packages
      become: true
      ansible.builtin.yum:
        name: ./{{ vector_version }}-1.x86_64.rpm
      changed_when: false
      tags:
        - vector
        - distr

    - name: Vector | Сonfig Service Vector
      become: true
      ansible.builtin.template:
        src: vector.service.j2
        dest: /usr/lib/systemd/system/vector.service
        mode: "0644"

    - name: Vector | Create a directory if it does not exist
      ansible.builtin.file:
        path: "{{ vector_config_dir }}"
        state: directory
        mode: "0755"

    - name: Deploy vector configuration
      become: true
      ansible.builtin.template:
        src: templates/vector.yaml.j2
        dest: "{{ vector_config_dir }}/vector.yml"
        mode: "0644"
      notify: Start vector service
      tags:
        - vector
        - config

    - name: Flush handlers
      ansible.builtin.meta: flush_handlers
      tags:
        - vector
        - restart service

# ######################################################

- name: Install nginx+epel-release
  hosts: lighthouse
  become: true
  handlers:
    - name: Start nginx service
      ansible.builtin.service:
        name: nginx
        state: restarted
    - name: start-nginx
      become: true
      command: nginx
    - name: reload-nginx
      become: true
      command: nginx -s reload

      tags:
        - nginx
        - start service

  tasks:
  # - block:
    - name: Install epel-release
      become: true
      ansible.builtin.yum:
        name: epel-release
        state: present
        update_cache: yes

    - name: Install nginx
      ansible.builtin.yum:
        name: nginx
        state: present
        update_cache: yes

    - name: Deploy nginx configuration
      become: true
      ansible.builtin.template:
        src: templates/nginx.conf.j2
        dest:  /etc/nginx/nginx.conf
        mode: '0644'
      notify: Start nginx service
      tags:
        - nginx
        - config

# ##########################################

- name: Install LightHouse
  hosts: lighthouse
  become: true
  # handlers:     #Сделал отдельным заданием внизу
  #   - name: reload-nginx
  #     become: true 
  #     command: nginx -s reload
  pre_tasks:
    - block:
        - name: Install git
          ansible.builtin.yum:
            name: git
            state: latest

  tasks:
        - name: Lighthouse cloned
          become: true
          ansible.builtin.git:
            repo: "{{ lighthouse_vcs }}"
            dest: "{{ lighthouse_dir }}" #тут была ошибка в адресе
            version: master

        - name: Deploy lighthouse configuration
          become: true
          ansible.builtin.template:
           src: templates/lighthouse.conf.j2
           dest: /etc/nginx/conf.d/default.conf
           mode: '0644'

        - name: Restart nginx service
          ansible.builtin.service:
            name: nginx
            state: restarted
          tags: Install LightHouse

        # - name: Start nginx service
        #   ansible.builtin.service:
        #     name: nginx
        #     state: restarted